# Your library: source EPUBs, rendered audiobooks, per-book settings.
# The pipeline creates books/<slug>/ for you; nothing in it is tracked.
books/

# Intermediates, regenerated by the pipeline
build/
samples/

.venv/
__pycache__/
*.pyc
.DS_Store

# Personal lexicons: only shared.tsv is tracked.
lexicon/*.tsv
!lexicon/shared.tsv
